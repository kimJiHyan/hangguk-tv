# 私人电视直播 APP 设计文档

## 应用概述

一款专为安卓电视和手机设计的直播应用，支持从 GitHub 加载直播源、云端授权控制、设备 ID 验证及收款码展示。

## 屏幕列表

| 屏幕名称 | 功能描述 | 关键元素 |
|---------|---------|---------|
| **启动屏** | 应用启动时显示 | Logo、加载动画 |
| **主屏** | 直播列表或单一直播源 | 直播流、播放控制、设备状态 |
| **播放屏** | 全屏直播播放 | 视频播放器、控制条、返回按钮 |
| **授权屏** | 访问被拒绝或过期 | 收款码、到期提示、重试按钮 |
| **错误屏** | 网络错误或加载失败 | 错误信息、重试按钮 |

## 主要内容与功能

### 主屏
- **直播源加载**：从 GitHub 的 `playlist.m3u` 读取直播源列表
- **播放列表**：显示可用的直播频道（如果有多个）
- **设备状态**：显示当前设备 ID 和授权状态
- **云端控制**：定期检查 GitHub `config.json` 中的全局开关状态

### 播放屏
- **视频播放**：使用 `expo-video` 播放 M3U8 直播流
- **全屏模式**：支持全屏播放
- **控制条**：播放/暂停、音量、返回按钮
- **遥控器支持**：方向键导航、OK 键确认、返回键退出

### 授权屏
- **收款码展示**：显示用户提供的微信收款二维码
- **授权提示**：清晰的文字说明（"您的访问已过期"或"该设备未授权"）
- **重试按钮**：允许用户重新检查授权状态

## 关键用户流程

### 流程 1：正常观看直播
1. 用户打开应用
2. 应用检查设备 ID 和授权状态（从 GitHub 读取）
3. 如果授权有效，显示直播列表
4. 用户选择直播频道
5. 应用加载并播放直播流
6. 用户可以暂停、调整音量、全屏观看

### 流程 2：访问被拒绝
1. 用户打开应用
2. 应用检查授权状态
3. 如果授权无效或过期，显示授权屏
4. 授权屏显示收款二维码和提示信息
5. 用户扫码支付后，应用重新检查授权状态
6. 授权成功后，返回主屏

### 流程 3：云端停止服务
1. 用户在 GitHub 的 `config.json` 中将 `globalStatus` 设置为 `false`
2. 应用定期检查此状态
3. 当检测到 `globalStatus` 为 `false` 时，立即显示授权屏
4. 用户无法继续观看，直到状态恢复

## 颜色方案

| 用途 | 颜色 | 说明 |
|-----|------|------|
| **背景** | `#0a0a0a`（深黑色） | 电视屏幕背景，适合长时间观看 |
| **主色** | `#ff6b35`（橙红色） | 强调色，用于按钮和焦点状态 |
| **文字** | `#ffffff`（白色） | 主要文字，高对比度 |
| **次文字** | `#b0b0b0`（灰色） | 辅助文字 |
| **成功** | `#22c55e`（绿色） | 授权成功、连接成功 |
| **警告** | `#f59e0b`（黄色） | 网络不稳定、缓冲中 |
| **错误** | `#ef4444`（红色） | 错误提示、访问被拒 |

## 设计原则

1. **简洁大气**：为电视屏幕优化，避免过多细节
2. **大字体**：字体大小不低于 16sp，便于远距离观看
3. **遥控器友好**：支持方向键导航，焦点清晰可见
4. **快速反馈**：按钮按下时有明显的视觉反馈
5. **无障碍**：高对比度，清晰的焦点指示

## 技术实现

- **框架**：Expo (React Native) SDK 54
- **样式**：NativeWind (Tailwind CSS)
- **视频播放**：expo-video
- **存储**：AsyncStorage（本地设备 ID）
- **网络**：axios（GitHub API 请求）
- **状态管理**：React Context + useState

## 下一步

1. 实现 GitHub 配置读取逻辑
2. 实现 M3U 播放列表解析
3. 集成视频播放器
4. 设计和实现 UI 组件
5. 测试和优化
